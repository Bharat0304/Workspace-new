{
  "version": 2,
  "buildCommand": "mkdir -p public && rsync -av frontend/ public/",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "public/**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
