{
  "version": 2,
  "buildCommand": "mkdir -p public && find frontend -type f -exec cp {} public/ \\;",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "public/**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
